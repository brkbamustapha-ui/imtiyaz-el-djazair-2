# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files — NEVER commit real secrets
.env
.env*.local
.env.production

# leftovers from the old SQLite setup — the database is PostgreSQL now
/prisma/*.db
/prisma/*.db-journal
/prisma/dev.db*

# typescript
*.tsbuildinfo

# vercel
.vercel

# uploads and form attachments now live in the database, not on disk
/storage/
